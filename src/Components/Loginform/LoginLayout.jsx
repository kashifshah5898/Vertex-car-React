import React from "react";

const LoginLayout = ({ children }) => {
  return (
    <div className="login-layout">
      {/* Place any additional styling or layout components here */}
      {children}
    </div>
  );
};

export default LoginLayout;
